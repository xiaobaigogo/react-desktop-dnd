import GridLayout from "react-grid-layout"
import {LayoutController} from "./LayoutController";

export type Props = {
    onLoad: (_ctr: LayoutController) => void
}

export default (props: Props) => (
    <div>123</div>
)